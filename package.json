{
  "name": "neu-web",
  "version": "1.0.0",
  "description": "New PCWA website",
  "main": "index.js",
  "repository": "pcwa.net",
  "author": "abe hendricks <ahendricks@pcwa.net>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "forecast:dev": "npm run server:dev --prefix packages/forecast",
    "mailjet:dev": "npm run dev --prefix packages/mailjet",
    "cosmic:dev": "npm run dev --prefix packages/cosmic",
    "next:dev": "npm run dev --prefix packages/next",
    "dev": "concurrently -n next,forecast,mailjet,cosmic 'npm run next:dev' 'npm run forecast:dev' 'npm run mailjet:dev' 'npm run cosmic:dev'",
    "serve": "concurrently -n next 'npm run serve --prefix packages/next'",
    "prenow": "npm run build --prefix packages/forecast && npm run build --prefix packages/mailjet && npm run build --prefix packages/cosmic",
    "now": "now --target production",
    "now:forms": "npm run prenow && now --target production -A now.forms.json",
    "now-build": "next build",
    "flow": "flow"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.1.0",
    "flow-bin": "^0.95.1",
    "now": "^15.0.0"
  }
}
