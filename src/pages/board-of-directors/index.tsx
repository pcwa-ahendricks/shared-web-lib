import BoardOfDirectorsIndexPage from './[district]'

/* This allows /board-of-directors to simply open the page without any particular Board District selected. I feel like this should be the default behavior, however, this index page was needed at the time of writing. */

export default BoardOfDirectorsIndexPage
